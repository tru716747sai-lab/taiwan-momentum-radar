[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "taiwan-momentum-radar"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "pandas>=2.2,<3", "numpy>=1.26,<3", "yfinance>=0.2.54",
  "PyYAML>=6.0", "lxml>=5.0", "html5lib>=1.1", "beautifulsoup4>=4.12"
]

[tool.setuptools.packages.find]
where = ["src"]
